namespace FinViet.Domain.Enums;

/// <summary>Maps to Postgres enum <c>notification_entity_type</c> (budget, goal, report, wallet, system).</summary>
public enum NotificationEntityType
{
    Budget,
    Goal,
    Report,
    Wallet,
    System
}
