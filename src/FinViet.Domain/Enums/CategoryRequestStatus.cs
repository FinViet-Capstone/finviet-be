namespace FinViet.Domain.Enums;

/// <summary>Maps to Postgres enum <c>category_request_status</c> (pending, approved, rejected).</summary>
public enum CategoryRequestStatus
{
    Pending,
    Approved,
    Rejected
}
